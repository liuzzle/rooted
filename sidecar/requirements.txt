# Ingestion worker dependencies.
#
# Install into the sidecar's own environment — the app finds it automatically:
#   python3 -m venv sidecar/.venv
#   sidecar/.venv/bin/python -m pip install -r sidecar/requirements.txt

# Phase 3 — typed documents. (.txt/.md/.docx need nothing beyond the stdlib.)
pypdf>=5.0

# Phase 4 — handwriting and scans, read on-device by macOS Vision.
# Also rasterises scanned PDFs so their pages can be OCR'd and reviewed.
pyobjc-framework-Vision>=10.0 ; sys_platform == "darwin"
pyobjc-framework-Quartz>=10.0 ; sys_platform == "darwin"

# Phase 4 — audio. Timestamped transcription, on device.
faster-whisper>=1.1

# Speaker labels are opt-in: pyannote pulls in torch, and needs a
# HUGGINGFACE_TOKEN whose account has accepted the conditions on all three of
# pyannote/speaker-diarization-3.1, pyannote/segmentation-3.0 and
# pyannote/speaker-diarization-community-1 (see .env.example). Without it a
# transcript simply doesn't say who was talking.
#   sidecar/.venv/bin/pip install "pyannote.audio>=3.1"

# Phase 4 — cloud second opinion on a scan the local OCR can't read. Opt-in:
# without it (and an ANTHROPIC_API_KEY) the option isn't offered at all, and
# nothing can leave the machine.
#   sidecar/.venv/bin/pip install anthropic
